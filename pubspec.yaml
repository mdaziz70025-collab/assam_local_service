name: basp
description: "Assam Local Service App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core & Auth
  firebase_core: ^2.27.0
  firebase_auth: ^4.16.0
  google_sign_in: 6.1.6

  # UI Components & Maps
  cupertino_icons: ^1.0.6
  google_maps_flutter: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  # Image path omitted so default flutter icon is used

flutter:
  uses-material-design: true

  assets:
    - assets/
